from django.apps import AppConfig


class PermisosConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "boldApp.permisos"
    label = "boldApp_permisos"
    verbose_name = "Bold — Permisos"

